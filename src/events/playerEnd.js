module.exports = async (client, player) => {
    console.log(`[${player.guildId}] Track ended`);
    
    // Auto-play next track is handled by Kazagumo automatically
    // This event is just for logging or additional logic
};

